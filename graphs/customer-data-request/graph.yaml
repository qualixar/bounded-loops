# customer-data-request — customer
#
# A customer data-subject request clears processor terms, privacy-notice completeness and a secret scan in parallel before a privacy officer authorises the response. A failed terms check routes to clause extraction so the gap can be named.
#
# GENERATED by scripts/regenerate_reference_graphs.py. Every loop_package below is a CONTENT
# digest of a shipped package under loops/, so editing one of those packages invalidates this
# file. tests/graphs/test_reference_graphs.py fails on that drift and names the script.
#
# Keyless: every loop node runs on a stub runner with its own real mechanical gate, so this
# graph needs no API key and no spend.
api_version: bounded-loops.dev/graph/v1
graph_id: customer-data-request
version: 1.0.0
connection_slots: []
nodes:
  - id: check-processor-terms
    kind: loop
    loop_package: "sha256:48d2e726f210737d0c1302e8d258c9439a2078256f9579f7d3a7a1036879449c"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: check-privacy-notice
    kind: loop
    loop_package: "sha256:1ecbe300efa268a2087aca08abc00c8c1c5b94e197f847fa8435184b96640861"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: scan-exposure
    kind: loop
    loop_package: "sha256:57122b3bc5191b72d937d7637bf5e7a2bdf90ad4341148f17d1368388e530c2a"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: extract-clauses
    kind: loop
    loop_package: "sha256:9059bff06169730e7b3e1de2b8e3bccfe0741dad6f0b8ff639fb2b5237bf7443"
    inputs:
      source: internal
    outputs:
      reconciliation: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: join-checks
    kind: join
    mode: all_successful
    inputs:
      check-processor-terms: internal
      check-privacy-notice: internal
      scan-exposure: internal
    outputs:
      cleared: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 60
    effects: []
    isolation: workspace_only
  - id: approve-customer
    kind: approval
    required_role: privacy-officer
    inputs:
      cleared: internal
    outputs:
      decision: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 86400
    effects: []
    isolation: workspace_only
  - id: publish-instruction
    kind: publish
    publication_policy: customer-instruction-v1
    inputs:
      decision: internal
    outputs:
      receipt: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects:
      - external_write
    isolation: container_restricted
edges:
  - from_node: check-processor-terms
    from_port: verdict
    to_node: join-checks
    to_port: check-processor-terms
    when: succeeded
  - from_node: check-privacy-notice
    from_port: verdict
    to_node: join-checks
    to_port: check-privacy-notice
    when: succeeded
  - from_node: scan-exposure
    from_port: verdict
    to_node: join-checks
    to_port: scan-exposure
    when: succeeded
  - from_node: check-processor-terms
    from_port: verdict
    to_node: extract-clauses
    to_port: source
    when: failed
  - from_node: join-checks
    from_port: cleared
    to_node: approve-customer
    to_port: cleared
  - from_node: approve-customer
    from_port: decision
    to_node: publish-instruction
    to_port: decision
policies:
  fail_mode: continue_declared
  data_class: internal
