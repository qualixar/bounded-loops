# retail-listing-release — retail
#
# A product listing clears barcode, feed-schema and margin checks in parallel before a merchandiser releases it to the storefront. A failed margin check routes to a stock review rather than stopping the release pipeline.
#
# GENERATED by scripts/regenerate_reference_graphs.py. Every loop_package below is a CONTENT
# digest of a shipped package under loops/, so editing one of those packages invalidates this
# file. tests/graphs/test_reference_graphs.py fails on that drift and names the script.
#
# Keyless: every loop node runs on a stub runner with its own real mechanical gate, so this
# graph needs no API key and no spend.
api_version: bounded-loops.dev/graph/v1
graph_id: retail-listing-release
version: 1.0.0
connection_slots: []
nodes:
  - id: check-barcode
    kind: loop
    loop_package: "sha256:d0aa494bfdcc3a3bccedc34c22b251d0348f8ee7fdfe1d7880432c73569d87b0"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: workspace_only
  - id: validate-feed
    kind: loop
    loop_package: "sha256:3257e7909f69cc03e4a4320b729167746c2de6d2851de0d02a4de41d1edc5e96"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: workspace_only
  - id: check-margin
    kind: loop
    loop_package: "sha256:dc4e57a84712937099f1f9a9450fe220c184ad91e3ab6afe9a7a91379adb93f0"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: workspace_only
  - id: review-stock
    kind: loop
    loop_package: "sha256:a9b1bb1e11363e0b2becb1d7641c49cdcaf6aa870d8f7837866f01d828de4a79"
    inputs:
      source: internal
    outputs:
      reconciliation: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: workspace_only
  - id: join-checks
    kind: join
    mode: all_successful
    inputs:
      check-barcode: internal
      validate-feed: internal
      check-margin: internal
    outputs:
      cleared: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 60
    effects: []
    isolation: workspace_only
  - id: approve-retail
    kind: approval
    required_role: merchandising-lead
    inputs:
      cleared: internal
    outputs:
      decision: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 86400
    effects: []
    isolation: workspace_only
  - id: publish-instruction
    kind: publish
    publication_policy: retail-instruction-v1
    inputs:
      decision: internal
    outputs:
      receipt: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects:
      - external_write
    isolation: container_restricted
edges:
  - from_node: check-barcode
    from_port: verdict
    to_node: join-checks
    to_port: check-barcode
    when: succeeded
  - from_node: validate-feed
    from_port: verdict
    to_node: join-checks
    to_port: validate-feed
    when: succeeded
  - from_node: check-margin
    from_port: verdict
    to_node: join-checks
    to_port: check-margin
    when: succeeded
  - from_node: check-margin
    from_port: verdict
    to_node: review-stock
    to_port: source
    when: failed
  - from_node: join-checks
    from_port: cleared
    to_node: approve-retail
    to_port: cleared
  - from_node: approve-retail
    from_port: decision
    to_node: publish-instruction
    to_port: decision
policies:
  fail_mode: continue_declared
  data_class: internal
