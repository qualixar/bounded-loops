# retail-listing-release — retail
#
# A product listing clears barcode, feed-schema and margin checks in parallel before a merchandiser releases it to the storefront. A failed margin check routes to a stock review rather than stopping the release pipeline.
#
# GENERATED by scripts/regenerate_reference_graphs.py. Every loop_package below is a CONTENT
# digest of a shipped package under loops/, so editing one of those packages invalidates this
# file. tests/graphs/test_reference_graphs.py fails on that drift and names the script.
#
# Keyless: every loop node runs on a stub runner with its own real mechanical gate, so this
# graph needs no API key and no spend.
api_version: bounded-loops.dev/graph/v1
graph_id: retail-listing-release
version: 1.0.0
connection_slots: []
nodes:
  - id: check-barcode
    kind: loop
    loop_package: "sha256:bba2dc49bbe6cd95e22caf3d13059843205b60aac6dc11911882bb8d79bde576"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: validate-feed
    kind: loop
    loop_package: "sha256:6f2dab5edb69eec04bd3bc815624f89fb9fa01d14ee4ba9d866902be22809adb"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: check-margin
    kind: loop
    loop_package: "sha256:15c393d703e37434dc066a2f7e750cfadcee2657a4b1c56deb63bed1f293d8da"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: review-stock
    kind: loop
    loop_package: "sha256:6e5b791d78cd20bd472d32fa2ac0ec27f8bde06804275e9a8532ce1dd75a660a"
    inputs:
      source: internal
    outputs:
      reconciliation: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: join-checks
    kind: join
    mode: all_successful
    inputs:
      check-barcode: internal
      validate-feed: internal
      check-margin: internal
    outputs:
      cleared: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 60
    effects: []
    isolation: workspace_only
  - id: approve-retail
    kind: approval
    required_role: merchandising-lead
    inputs:
      cleared: internal
    outputs:
      decision: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 86400
    effects: []
    isolation: workspace_only
  - id: publish-instruction
    kind: publish
    publication_policy: retail-instruction-v1
    inputs:
      decision: internal
    outputs:
      receipt: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects:
      - external_write
    isolation: container_restricted
edges:
  - from_node: check-barcode
    from_port: verdict
    to_node: join-checks
    to_port: check-barcode
    when: succeeded
  - from_node: validate-feed
    from_port: verdict
    to_node: join-checks
    to_port: validate-feed
    when: succeeded
  - from_node: check-margin
    from_port: verdict
    to_node: join-checks
    to_port: check-margin
    when: succeeded
  - from_node: check-margin
    from_port: verdict
    to_node: review-stock
    to_port: source
    when: failed
  - from_node: join-checks
    from_port: cleared
    to_node: approve-retail
    to_port: cleared
  - from_node: approve-retail
    from_port: decision
    to_node: publish-instruction
    to_port: decision
policies:
  fail_mode: continue_declared
  data_class: internal
