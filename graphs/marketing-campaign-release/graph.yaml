# marketing-campaign-release — marketing
#
# Campaign copy clears SEO limits, reading level and image alt-text in parallel, then an editor approves publication. A failed factual claim routes to source mapping instead of killing the campaign.
#
# GENERATED by scripts/regenerate_reference_graphs.py. Every loop_package below is a CONTENT
# digest of a shipped package under loops/, so editing one of those packages invalidates this
# file. tests/graphs/test_reference_graphs.py fails on that drift and names the script.
#
# Keyless: every loop node runs on a stub runner with its own real mechanical gate, so this
# graph needs no API key and no spend.
api_version: bounded-loops.dev/graph/v1
graph_id: marketing-campaign-release
version: 1.0.0
connection_slots: []
nodes:
  - id: check-seo
    kind: loop
    loop_package: "sha256:9e8c7f1cf01082378bebcfd50966efbf46c4aa49dd099d13c26c97b2b78ed5a3"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: workspace_only
  - id: check-reading-level
    kind: loop
    loop_package: "sha256:4d57216996a8ec9065c89b11a75d03a39abcfefccf43c0f1fd251bc5d21f4120"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: workspace_only
  - id: check-alt-text
    kind: loop
    loop_package: "sha256:7e7bb4873d8d27ce8bef30599230c0c36d809b8862718bfd4e8f41c9d08a0df5"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: workspace_only
  - id: map-claim-sources
    kind: loop
    loop_package: "sha256:368a4f77834a977fd5a5a2b538f05587c60570e8ec7be3434c782159e37cc1e0"
    inputs:
      source: internal
    outputs:
      reconciliation: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: workspace_only
  - id: join-checks
    kind: join
    mode: all_successful
    inputs:
      check-seo: internal
      check-reading-level: internal
      check-alt-text: internal
    outputs:
      cleared: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 60
    effects: []
    isolation: workspace_only
  - id: approve-marketing
    kind: approval
    required_role: content-editor
    inputs:
      cleared: internal
    outputs:
      decision: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 86400
    effects: []
    isolation: workspace_only
  - id: publish-instruction
    kind: publish
    publication_policy: marketing-instruction-v1
    inputs:
      decision: internal
    outputs:
      receipt: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects:
      - external_write
    isolation: container_restricted
edges:
  - from_node: check-seo
    from_port: verdict
    to_node: join-checks
    to_port: check-seo
    when: succeeded
  - from_node: check-reading-level
    from_port: verdict
    to_node: join-checks
    to_port: check-reading-level
    when: succeeded
  - from_node: check-alt-text
    from_port: verdict
    to_node: join-checks
    to_port: check-alt-text
    when: succeeded
  - from_node: check-reading-level
    from_port: verdict
    to_node: map-claim-sources
    to_port: source
    when: failed
  - from_node: join-checks
    from_port: cleared
    to_node: approve-marketing
    to_port: cleared
  - from_node: approve-marketing
    from_port: decision
    to_node: publish-instruction
    to_port: decision
policies:
  fail_mode: continue_declared
  data_class: internal
