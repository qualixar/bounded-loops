# marketing-campaign-release — marketing
#
# Campaign copy clears SEO limits, reading level and image alt-text in parallel, then an editor approves publication. Missing alt text routes to claim-source mapping, since both are evidence gaps in the same asset, instead of killing the campaign.
#
# GENERATED by scripts/regenerate_reference_graphs.py. Every loop_package below is a CONTENT
# digest of a shipped package under loops/, so editing one of those packages invalidates this
# file. tests/graphs/test_reference_graphs.py fails on that drift and names the script.
#
# Keyless: every loop node runs on a stub runner with its own real mechanical gate, so this
# graph needs no API key and no spend.
api_version: bounded-loops.dev/graph/v1
graph_id: marketing-campaign-release
version: 1.0.0
connection_slots: []
nodes:
  - id: check-seo
    kind: loop
    loop_package: "sha256:181d84a6308d3f42f5898706f071ebf73a13aad99554c076784774f30dfcfde5"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: check-reading-level
    kind: loop
    loop_package: "sha256:af3c8d32036a7f8ccff2d48fede4bda93fa9e6d82c7f35265acafc45974afa44"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: check-alt-text
    kind: loop
    loop_package: "sha256:a25dcd89fc62af1ef5f3cad14ba78074ec2577a5a96fbb26078d9efeac0d9009"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: map-claim-sources
    kind: loop
    loop_package: "sha256:0e2a37de06e9fbe1926966dbbcca8d0df5b6a71dcbff44f2ac272842233fcc66"
    inputs:
      source: internal
    outputs:
      reconciliation: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: join-checks
    kind: join
    mode: all_successful
    inputs:
      check-seo: internal
      check-reading-level: internal
      check-alt-text: internal
    outputs:
      cleared: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 60
    effects: []
    isolation: workspace_only
  - id: approve-marketing
    kind: approval
    required_role: content-editor
    inputs:
      cleared: internal
    outputs:
      decision: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 86400
    effects: []
    isolation: workspace_only
  - id: publish-instruction
    kind: publish
    publication_policy: marketing-instruction-v1
    inputs:
      decision: internal
    outputs:
      receipt: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects:
      - external_write
    isolation: container_restricted
edges:
  - from_node: check-seo
    from_port: verdict
    to_node: join-checks
    to_port: check-seo
    when: succeeded
  - from_node: check-reading-level
    from_port: verdict
    to_node: join-checks
    to_port: check-reading-level
    when: succeeded
  - from_node: check-alt-text
    from_port: verdict
    to_node: join-checks
    to_port: check-alt-text
    when: succeeded
  - from_node: check-alt-text
    from_port: verdict
    to_node: map-claim-sources
    to_port: source
    when: failed
  - from_node: join-checks
    from_port: cleared
    to_node: approve-marketing
    to_port: cleared
  - from_node: approve-marketing
    from_port: decision
    to_node: publish-instruction
    to_port: decision
policies:
  fail_mode: continue_declared
  data_class: internal
