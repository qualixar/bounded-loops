# marketing-campaign-release — marketing
#
# Campaign copy clears SEO limits, reading level and image alt-text in parallel, then an editor approves publication. Missing alt text routes to claim-source mapping, since both are evidence gaps in the same asset, instead of killing the campaign.
#
# GENERATED by scripts/regenerate_reference_graphs.py. Every loop_package below is a CONTENT
# digest of a shipped package under loops/, so editing one of those packages invalidates this
# file. tests/graphs/test_reference_graphs.py fails on that drift and names the script.
#
# Keyless: every loop node runs on a stub runner with its own real mechanical gate, so this
# graph needs no API key and no spend.
api_version: bounded-loops.dev/graph/v1
graph_id: marketing-campaign-release
version: 1.0.0
connection_slots: []
nodes:
  - id: check-seo
    kind: loop
    loop_package: "sha256:35746763a76dc6e3fe051ac96c3957239a8301eb20195197aaa6815269a3be32"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: check-reading-level
    kind: loop
    loop_package: "sha256:66c807e2fe3e7ade0a5e2e56e298778ad3ef7f98e277329bd3034214491b3be3"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: check-alt-text
    kind: loop
    loop_package: "sha256:91f5c6e24abe804c48e1e0d37bc9e75b430ddcc337db7469ac13b9cf0cd29ccd"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: map-claim-sources
    kind: loop
    loop_package: "sha256:df0740c57dd75329cee269e7c00dcb48c8390d34edfc662fcf93a07334af596c"
    inputs:
      source: internal
    outputs:
      reconciliation: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: join-checks
    kind: join
    mode: all_successful
    inputs:
      check-seo: internal
      check-reading-level: internal
      check-alt-text: internal
    outputs:
      cleared: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 60
    effects: []
    isolation: workspace_only
  - id: approve-marketing
    kind: approval
    required_role: content-editor
    inputs:
      cleared: internal
    outputs:
      decision: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 86400
    effects: []
    isolation: workspace_only
  - id: publish-instruction
    kind: publish
    publication_policy: marketing-instruction-v1
    inputs:
      decision: internal
    outputs:
      receipt: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects:
      - external_write
    isolation: container_restricted
edges:
  - from_node: check-seo
    from_port: verdict
    to_node: join-checks
    to_port: check-seo
    when: succeeded
  - from_node: check-reading-level
    from_port: verdict
    to_node: join-checks
    to_port: check-reading-level
    when: succeeded
  - from_node: check-alt-text
    from_port: verdict
    to_node: join-checks
    to_port: check-alt-text
    when: succeeded
  - from_node: check-alt-text
    from_port: verdict
    to_node: map-claim-sources
    to_port: source
    when: failed
  - from_node: join-checks
    from_port: cleared
    to_node: approve-marketing
    to_port: cleared
  - from_node: approve-marketing
    from_port: decision
    to_node: publish-instruction
    to_port: decision
policies:
  fail_mode: continue_declared
  data_class: internal
