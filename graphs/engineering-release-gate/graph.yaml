# engineering-release-gate — engineering
#
# A release candidate clears dependency pinning, CORS posture and container-user checks in parallel before a release manager cuts the tag. A failed CORS check routes to a secret scan, on the reasoning that a permissive origin policy rarely travels alone.
#
# GENERATED by scripts/regenerate_reference_graphs.py. Every loop_package below is a CONTENT
# digest of a shipped package under loops/, so editing one of those packages invalidates this
# file. tests/graphs/test_reference_graphs.py fails on that drift and names the script.
#
# Keyless: every loop node runs on a stub runner with its own real mechanical gate, so this
# graph needs no API key and no spend.
api_version: bounded-loops.dev/graph/v1
graph_id: engineering-release-gate
version: 1.0.0
connection_slots: []
nodes:
  - id: pin-dependencies
    kind: loop
    loop_package: "sha256:653899a20c61919b5c8280163cc16e698df652839c72a4e74ad8c2da0860897d"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: check-cors
    kind: loop
    loop_package: "sha256:c834f444c98bd2663eb624aaf8b329d1573c8fbbd2c0e1bbba4fc4af81c3176b"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: check-container-user
    kind: loop
    loop_package: "sha256:9b5ea9ecda3ac33e638378d7c5e60fa0b9a1d355d13088fbb1aa86ef2f2c0177"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: scan-secrets
    kind: loop
    loop_package: "sha256:d49619cdc18ecb059978c139c71652e0a0044b630e9f11a67051457d293f52b5"
    inputs:
      source: internal
    outputs:
      reconciliation: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: join-checks
    kind: join
    mode: all_successful
    inputs:
      pin-dependencies: internal
      check-cors: internal
      check-container-user: internal
    outputs:
      cleared: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 60
    effects: []
    isolation: workspace_only
  - id: approve-engineering
    kind: approval
    required_role: release-manager
    inputs:
      cleared: internal
    outputs:
      decision: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 86400
    effects: []
    isolation: workspace_only
  - id: publish-instruction
    kind: publish
    publication_policy: engineering-instruction-v1
    inputs:
      decision: internal
    outputs:
      receipt: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects:
      - external_write
    isolation: container_restricted
edges:
  - from_node: pin-dependencies
    from_port: verdict
    to_node: join-checks
    to_port: pin-dependencies
    when: succeeded
  - from_node: check-cors
    from_port: verdict
    to_node: join-checks
    to_port: check-cors
    when: succeeded
  - from_node: check-container-user
    from_port: verdict
    to_node: join-checks
    to_port: check-container-user
    when: succeeded
  - from_node: check-cors
    from_port: verdict
    to_node: scan-secrets
    to_port: source
    when: failed
  - from_node: join-checks
    from_port: cleared
    to_node: approve-engineering
    to_port: cleared
  - from_node: approve-engineering
    from_port: decision
    to_node: publish-instruction
    to_port: decision
policies:
  fail_mode: continue_declared
  data_class: internal
