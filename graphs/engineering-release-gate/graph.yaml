# engineering-release-gate — engineering
#
# A release candidate clears dependency pinning, CORS posture and container-user checks in parallel before a release manager cuts the tag. A failed CORS check routes to a secret scan, on the reasoning that a permissive origin policy rarely travels alone.
#
# GENERATED by scripts/regenerate_reference_graphs.py. Every loop_package below is a CONTENT
# digest of a shipped package under loops/, so editing one of those packages invalidates this
# file. tests/graphs/test_reference_graphs.py fails on that drift and names the script.
#
# Keyless: every loop node runs on a stub runner with its own real mechanical gate, so this
# graph needs no API key and no spend.
api_version: bounded-loops.dev/graph/v1
graph_id: engineering-release-gate
version: 1.0.0
connection_slots: []
nodes:
  - id: pin-dependencies
    kind: loop
    loop_package: "sha256:5a8254f0c067fe9dcdaea023e1e3573feab05518b567eeee594e3b49e7aaa44f"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: check-cors
    kind: loop
    loop_package: "sha256:42080504098e8f2ff72bd0c0c4dcc3a74ad63a1f7c75446c5d035d8507ed76eb"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: check-container-user
    kind: loop
    loop_package: "sha256:71c1e6497c58e8d9b67837c861f7f5b8fc2c02b88d624e2f4e8ff0308e4d53fb"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: scan-secrets
    kind: loop
    loop_package: "sha256:5e57e4997ddedc33f81fffe1fad04e98c8fb3ccc8a8eab84ca7b74f7190a0acb"
    inputs:
      source: internal
    outputs:
      reconciliation: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: join-checks
    kind: join
    mode: all_successful
    inputs:
      pin-dependencies: internal
      check-cors: internal
      check-container-user: internal
    outputs:
      cleared: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 60
    effects: []
    isolation: workspace_only
  - id: approve-engineering
    kind: approval
    required_role: release-manager
    inputs:
      cleared: internal
    outputs:
      decision: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 86400
    effects: []
    isolation: workspace_only
  - id: publish-instruction
    kind: publish
    publication_policy: engineering-instruction-v1
    inputs:
      decision: internal
    outputs:
      receipt: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects:
      - external_write
    isolation: container_restricted
edges:
  - from_node: pin-dependencies
    from_port: verdict
    to_node: join-checks
    to_port: pin-dependencies
    when: succeeded
  - from_node: check-cors
    from_port: verdict
    to_node: join-checks
    to_port: check-cors
    when: succeeded
  - from_node: check-container-user
    from_port: verdict
    to_node: join-checks
    to_port: check-container-user
    when: succeeded
  - from_node: check-cors
    from_port: verdict
    to_node: scan-secrets
    to_port: source
    when: failed
  - from_node: join-checks
    from_port: cleared
    to_node: approve-engineering
    to_port: cleared
  - from_node: approve-engineering
    from_port: decision
    to_node: publish-instruction
    to_port: decision
policies:
  fail_mode: continue_declared
  data_class: internal
