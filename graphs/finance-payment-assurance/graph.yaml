# finance-payment-assurance — finance
#
# Three independent accounting checks run in parallel, join, and gate a payment instruction behind a controller's approval. A failed balance check routes to reconciliation instead of stopping the run.
#
# GENERATED by scripts/regenerate_reference_graphs.py. Every loop_package below is a CONTENT
# digest of a shipped package under loops/, so editing one of those packages invalidates this
# file. tests/graphs/test_reference_graphs.py fails on that drift and names the script.
#
# Keyless: every loop node runs on a stub runner with its own real mechanical gate, so this
# graph needs no API key and no spend.
api_version: bounded-loops.dev/graph/v1
graph_id: finance-payment-assurance
version: 1.0.0
connection_slots: []
nodes:
  - id: match-invoice
    kind: loop
    loop_package: "sha256:25b481caecc10258787d6a0dea768f37312b326e1896ffd24618cf4e62434d52"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: balance-journal
    kind: loop
    loop_package: "sha256:a956879087e7bd77b00bfa7320fcbcbe09f24a573b71d5417f9b094ba3c71bb7"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: check-fx-rate
    kind: loop
    loop_package: "sha256:e2c88b1bcf45746beb1ed31574ed592c593b48b65006411b3037a5c555f88b0b"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: reconcile-ledger
    kind: loop
    loop_package: "sha256:888ded3fe20a52632340e3fc40635d3f353856f958a2cf641488e6fc392c72e3"
    inputs:
      source: internal
    outputs:
      reconciliation: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: join-checks
    kind: join
    mode: all_successful
    inputs:
      match-invoice: internal
      balance-journal: internal
      check-fx-rate: internal
    outputs:
      cleared: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 60
    effects: []
    isolation: workspace_only
  - id: approve-finance
    kind: approval
    required_role: finance-controller
    inputs:
      cleared: internal
    outputs:
      decision: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 86400
    effects: []
    isolation: workspace_only
  - id: publish-instruction
    kind: publish
    publication_policy: finance-instruction-v1
    inputs:
      decision: internal
    outputs:
      receipt: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects:
      - external_write
    isolation: container_restricted
edges:
  - from_node: match-invoice
    from_port: verdict
    to_node: join-checks
    to_port: match-invoice
    when: succeeded
  - from_node: balance-journal
    from_port: verdict
    to_node: join-checks
    to_port: balance-journal
    when: succeeded
  - from_node: check-fx-rate
    from_port: verdict
    to_node: join-checks
    to_port: check-fx-rate
    when: succeeded
  - from_node: balance-journal
    from_port: verdict
    to_node: reconcile-ledger
    to_port: source
    when: failed
  - from_node: join-checks
    from_port: cleared
    to_node: approve-finance
    to_port: cleared
  - from_node: approve-finance
    from_port: decision
    to_node: publish-instruction
    to_port: decision
policies:
  fail_mode: continue_declared
  data_class: internal
