# operations-incident-gate — operations
#
# An SRE incident-response release clears SLO error-budget, on-call coverage and runbook completeness in parallel before a site-reliability lead approves the incident plan. A failed on-call check routes to alert-runbook-link verification; if that verification also fails, a single repair round re-runs the on-call check and its descendants so the graph retries rather than sealing a potentially-transient gap.
#
# GENERATED by scripts/regenerate_reference_graphs.py. Every loop_package below is a CONTENT
# digest of a shipped package under loops/, so editing one of those packages invalidates this
# file. tests/graphs/test_reference_graphs.py fails on that drift and names the script.
#
# Keyless: every loop node runs on a stub runner with its own real mechanical gate, so this
# graph needs no API key and no spend.
api_version: bounded-loops.dev/graph/v1
graph_id: operations-incident-gate
version: 1.0.0
connection_slots: []
nodes:
  - id: check-slo
    kind: loop
    loop_package: "sha256:fd83f941078b9a30ebcddc01209b53e454a3d7381424ed97f2cab2c201e277d0"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: check-oncall
    kind: loop
    loop_package: "sha256:8e0ae435c8b0c1e0b37a0629302e5c4dfdd5cd4a4a3ca61bcd65f62ecd90dcb0"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: check-runbook
    kind: loop
    loop_package: "sha256:0984f35eaec65adf2cdf47f8006bd7883761b118f910c8820c76ba2907c36bb0"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: link-runbook
    kind: loop
    loop_package: "sha256:6311b2f23773b9c5f676070c92395dd9df7472046c088c4951f8af674975317b"
    inputs:
      source: internal
    outputs:
      reconciliation: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    on_failure:
      mode: repair
      target: check-oncall
    isolation: process_restricted
  - id: join-checks
    kind: join
    mode: all_successful
    inputs:
      check-slo: internal
      check-oncall: internal
      check-runbook: internal
    outputs:
      cleared: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 60
    effects: []
    isolation: workspace_only
  - id: approve-operations
    kind: approval
    required_role: sre-lead
    inputs:
      cleared: internal
    outputs:
      decision: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 86400
    effects: []
    isolation: workspace_only
  - id: publish-instruction
    kind: publish
    publication_policy: operations-instruction-v1
    inputs:
      decision: internal
    outputs:
      receipt: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects:
      - external_write
    isolation: container_restricted
edges:
  - from_node: check-slo
    from_port: verdict
    to_node: join-checks
    to_port: check-slo
    when: succeeded
  - from_node: check-oncall
    from_port: verdict
    to_node: join-checks
    to_port: check-oncall
    when: succeeded
  - from_node: check-runbook
    from_port: verdict
    to_node: join-checks
    to_port: check-runbook
    when: succeeded
  - from_node: check-oncall
    from_port: verdict
    to_node: link-runbook
    to_port: source
    when: failed
  - from_node: join-checks
    from_port: cleared
    to_node: approve-operations
    to_port: cleared
  - from_node: approve-operations
    from_port: decision
    to_node: publish-instruction
    to_port: decision
policies:
  fail_mode: continue_declared
  data_class: internal
  repair_budget: 1
