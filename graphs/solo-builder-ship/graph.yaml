# solo-builder-ship — personal
#
# A solo builder's change clears test presence, type annotations and commit convention in parallel before they approve their own ship — the commit history is what the release notes are generated from. A missing test routes to a red-green fix loop.
#
# GENERATED by scripts/regenerate_reference_graphs.py. Every loop_package below is a CONTENT
# digest of a shipped package under loops/, so editing one of those packages invalidates this
# file. tests/graphs/test_reference_graphs.py fails on that drift and names the script.
#
# Keyless: every loop node runs on a stub runner with its own real mechanical gate, so this
# graph needs no API key and no spend.
api_version: bounded-loops.dev/graph/v1
graph_id: solo-builder-ship
version: 1.0.0
connection_slots: []
nodes:
  - id: check-tests-exist
    kind: loop
    loop_package: "sha256:40d418c93a038caf3720ffb9765090b7495c0b0865e70320f710cb2577a19224"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: check-types
    kind: loop
    loop_package: "sha256:b829f4dd73d167db860b24031b4d4914bbb8a700773af7fe351245158dfce038"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: check-commits
    kind: loop
    loop_package: "sha256:90df97edf9a209354ef804ffd7ea3842d8e127e8e3168ad25fe76cfe26b41aaf"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: fix-red-green
    kind: loop
    loop_package: "sha256:3f71605f7598b3442e4ffc2fc9f3934c40a649b5a33714009afae2b300f4998a"
    inputs:
      source: internal
    outputs:
      reconciliation: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: join-checks
    kind: join
    mode: all_successful
    inputs:
      check-tests-exist: internal
      check-types: internal
      check-commits: internal
    outputs:
      cleared: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 60
    effects: []
    isolation: workspace_only
  - id: approve-personal
    kind: approval
    required_role: maintainer
    inputs:
      cleared: internal
    outputs:
      decision: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 86400
    effects: []
    isolation: workspace_only
  - id: publish-instruction
    kind: publish
    publication_policy: personal-instruction-v1
    inputs:
      decision: internal
    outputs:
      receipt: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects:
      - external_write
    isolation: container_restricted
edges:
  - from_node: check-tests-exist
    from_port: verdict
    to_node: join-checks
    to_port: check-tests-exist
    when: succeeded
  - from_node: check-types
    from_port: verdict
    to_node: join-checks
    to_port: check-types
    when: succeeded
  - from_node: check-commits
    from_port: verdict
    to_node: join-checks
    to_port: check-commits
    when: succeeded
  - from_node: check-tests-exist
    from_port: verdict
    to_node: fix-red-green
    to_port: source
    when: failed
  - from_node: join-checks
    from_port: cleared
    to_node: approve-personal
    to_port: cleared
  - from_node: approve-personal
    from_port: decision
    to_node: publish-instruction
    to_port: decision
policies:
  fail_mode: continue_declared
  data_class: internal
