# solo-builder-ship — personal
#
# A solo builder's change clears test presence, type annotations and commit convention in parallel before they approve their own ship — the commit history is what the release notes are generated from. A missing test routes to a red-green fix loop.
#
# GENERATED by scripts/regenerate_reference_graphs.py. Every loop_package below is a CONTENT
# digest of a shipped package under loops/, so editing one of those packages invalidates this
# file. tests/graphs/test_reference_graphs.py fails on that drift and names the script.
#
# Keyless: every loop node runs on a stub runner with its own real mechanical gate, so this
# graph needs no API key and no spend.
api_version: bounded-loops.dev/graph/v1
graph_id: solo-builder-ship
version: 1.0.0
connection_slots: []
nodes:
  - id: check-tests-exist
    kind: loop
    loop_package: "sha256:e342bf11a78e398aec751de42280307e9e880af4f0973fbee187621334df6b04"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: check-types
    kind: loop
    loop_package: "sha256:9dc28df1508d011b2d81e2c8d0ac6f6cb0d4b74a0a47991f9ce2bebb6e201126"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: check-commits
    kind: loop
    loop_package: "sha256:e322142fba549b0a803336ee099fefcf92b95a2a9e36980fcdc3aab8030b9299"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: fix-red-green
    kind: loop
    loop_package: "sha256:8ddb05cdf013493addd2e8bd44076a60cb262967a6a0ce89f8662e82d58dd095"
    inputs:
      source: internal
    outputs:
      reconciliation: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: join-checks
    kind: join
    mode: all_successful
    inputs:
      check-tests-exist: internal
      check-types: internal
      check-commits: internal
    outputs:
      cleared: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 60
    effects: []
    isolation: workspace_only
  - id: approve-personal
    kind: approval
    required_role: maintainer
    inputs:
      cleared: internal
    outputs:
      decision: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 86400
    effects: []
    isolation: workspace_only
  - id: publish-instruction
    kind: publish
    publication_policy: personal-instruction-v1
    inputs:
      decision: internal
    outputs:
      receipt: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects:
      - external_write
    isolation: container_restricted
edges:
  - from_node: check-tests-exist
    from_port: verdict
    to_node: join-checks
    to_port: check-tests-exist
    when: succeeded
  - from_node: check-types
    from_port: verdict
    to_node: join-checks
    to_port: check-types
    when: succeeded
  - from_node: check-commits
    from_port: verdict
    to_node: join-checks
    to_port: check-commits
    when: succeeded
  - from_node: check-tests-exist
    from_port: verdict
    to_node: fix-red-green
    to_port: source
    when: failed
  - from_node: join-checks
    from_port: cleared
    to_node: approve-personal
    to_port: cleared
  - from_node: approve-personal
    from_port: decision
    to_node: publish-instruction
    to_port: decision
policies:
  fail_mode: continue_declared
  data_class: internal
