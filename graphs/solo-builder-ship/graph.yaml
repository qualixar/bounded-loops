# solo-builder-ship — personal
#
# A solo builder's change clears measurable objectives, roadmap fields and commit convention in parallel before they approve their own ship. A failed acceptance-criteria check routes to a red-green fix loop.
#
# GENERATED by scripts/regenerate_reference_graphs.py. Every loop_package below is a CONTENT
# digest of a shipped package under loops/, so editing one of those packages invalidates this
# file. tests/graphs/test_reference_graphs.py fails on that drift and names the script.
#
# Keyless: every loop node runs on a stub runner with its own real mechanical gate, so this
# graph needs no API key and no spend.
api_version: bounded-loops.dev/graph/v1
graph_id: solo-builder-ship
version: 1.0.0
connection_slots: []
nodes:
  - id: check-objectives
    kind: loop
    loop_package: "sha256:c9149abbcf81e185d096f79b401c5903586143c403239a58f4945224392b912b"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: check-roadmap-fields
    kind: loop
    loop_package: "sha256:c90b62306b6aee94de8cf5d27e8ed958adf03f955c75aec702c9e706aca767c1"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: check-commits
    kind: loop
    loop_package: "sha256:6046de40dd75a48583675806a82d7ea678c1937e71913b94e16f2111c950e311"
    inputs: {}
    outputs:
      verdict: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: fix-red-green
    kind: loop
    loop_package: "sha256:e4366a8a4370600f8a42632f8d25dae52f2356b889d28b649d82660fc3d1b4c3"
    inputs:
      source: internal
    outputs:
      reconciliation: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects: []
    isolation: process_restricted
  - id: join-checks
    kind: join
    mode: all_successful
    inputs:
      check-objectives: internal
      check-roadmap-fields: internal
      check-commits: internal
    outputs:
      cleared: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 60
    effects: []
    isolation: workspace_only
  - id: approve-personal
    kind: approval
    required_role: maintainer
    inputs:
      cleared: internal
    outputs:
      decision: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 86400
    effects: []
    isolation: workspace_only
  - id: publish-instruction
    kind: publish
    publication_policy: personal-instruction-v1
    inputs:
      decision: internal
    outputs:
      receipt: internal
    budget:
      max_attempts: 1
      max_wallclock_s: 300
    effects:
      - external_write
    isolation: container_restricted
edges:
  - from_node: check-objectives
    from_port: verdict
    to_node: join-checks
    to_port: check-objectives
    when: succeeded
  - from_node: check-roadmap-fields
    from_port: verdict
    to_node: join-checks
    to_port: check-roadmap-fields
    when: succeeded
  - from_node: check-commits
    from_port: verdict
    to_node: join-checks
    to_port: check-commits
    when: succeeded
  - from_node: check-objectives
    from_port: verdict
    to_node: fix-red-green
    to_port: source
    when: failed
  - from_node: join-checks
    from_port: cleared
    to_node: approve-personal
    to_port: cleared
  - from_node: approve-personal
    from_port: decision
    to_node: publish-instruction
    to_port: decision
policies:
  fail_mode: continue_declared
  data_class: internal
