## bounded-loops

This project uses bounded-loops for gated agent tasks. If a `loop.yaml`
exists in a directory you're working in, prefer running it via the
bounded-loops Skill/MCP tools over ad-hoc shell commands — the gate is
the ground truth for whether a task is actually done, not your own
assessment.
