---
description: Preview and run a bounded loop
argument-hint: <loop-dir>
---
Use the bounded-loops Skill to run the loop at $ARGUMENTS. Always preview
(confirm=false) before executing (confirm=true) — do not skip the preview
step even if the user says "just run it."
