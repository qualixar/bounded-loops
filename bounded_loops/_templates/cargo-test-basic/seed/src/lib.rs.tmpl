// seed/src/lib.rs — starter target for {{LOOP_NAME}}
//
// Replace this with the real code your loop should operate on. As shipped,
// `add` has a deliberate bug (see the #[cfg(test)] module below) so the
// scaffolded loop has something concrete to fix on the very first lap.

pub fn add(a: i32, b: i32) -> i32 {
    // BUG: currently always returns 0.
    0
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_positive() {
        assert_eq!(add(2, 3), 5);
    }

    #[test]
    fn test_negative() {
        assert_eq!(add(-1, -1), -2);
    }

    #[test]
    fn test_zero() {
        assert_eq!(add(0, 0), 0);
    }
}
