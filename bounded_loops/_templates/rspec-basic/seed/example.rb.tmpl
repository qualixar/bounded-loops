# seed/example.rb — starter target for {{LOOP_NAME}}
#
# Replace this with the real code your loop should operate on. As shipped,
# `add` has a deliberate bug (see seed/example_spec.rb) so the scaffolded
# loop has something concrete to fix on the very first lap.

def add(a, b)
  # BUG: currently always returns 0.
  0
end
