name: {{LOOP_NAME}}
description: >
  An RSpec-gated bounded loop, scaffolded via `bl new rspec-basic`.
pattern: evaluator-optimizer
role: [backend]
rung: L2
spec: PROMPT.md
runner:
  default: stub
gate:
  kind: command
  run: "rspec"
bounds: bounds.yaml
memory: STATE.md
