{
  "version": 1,
  "loop": "{{LOOP_NAME}}",
  "description": "Lap 1 writes the correct add() fix directly (keyless demo: the stub IS the 'agent'). rspec goes green on lap 1.",
  "interactions": [
    {
      "lap": 1,
      "agent_output": "Read seed/example_spec.rb. add() always returns 0. Fixing to return a + b.",
      "actions": [
        {
          "type": "write_file",
          "path": "seed/example.rb",
          "content": "def add(a, b)\n  a + b\nend\n"
        }
      ],
      "agent_claimed_done": true,
      "changed": true,
      "tokens": 0,
      "notes": "The fix — rspec now exits 0. bl run reaches DONE on lap 1, keyless."
    },
    {
      "lap": "*",
      "agent_output": "Fix already applied; no further changes needed.",
      "actions": [{"type": "noop"}],
      "agent_claimed_done": true,
      "changed": false,
      "tokens": 0,
      "notes": "Fallback for any lap beyond 1 — should not be reached in the happy path."
    }
  ]
}
