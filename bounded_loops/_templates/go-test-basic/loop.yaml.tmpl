name: {{LOOP_NAME}}
description: >
  A go-test-gated bounded loop, scaffolded via `bl new go-test-basic`.
pattern: evaluator-optimizer
role: [backend]
rung: L2
spec: PROMPT.md
runner:
  default: stub
gate:
  kind: command
  run: "cd seed && go test ./..."
bounds: bounds.yaml
memory: STATE.md
