// seed/example.js — starter target for {{LOOP_NAME}}
//
// Replace this with the real code your loop should operate on. As shipped,
// `add` has a deliberate bug (see seed/example.test.js) so the scaffolded
// loop has something concrete to fix on the very first lap.

/**
 * Add two integers. BUG: currently always returns 0.
 * @param {number} a
 * @param {number} b
 * @returns {number}
 */
function add(a, b) {
  return 0;
}

module.exports = { add };
