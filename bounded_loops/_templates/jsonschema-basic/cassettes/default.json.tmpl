{
  "version": 1,
  "loop": "{{LOOP_NAME}}",
  "description": "Lap 1 writes a corrected output.json: converts status from 'unknown' to 'ok', and version from the string '2' to the integer 2. JsonSchemaGate validates cleanly on lap 1. Demonstrates the canonical loop reaching DONE with zero API keys.",
  "interactions": [
    {
      "lap": 1,
      "agent_output": "Read schema.json: status must be ok/degraded/down, version must be integer >=1. Read seed/output.json: status='unknown' (invalid) and version='2' (string, not integer). Writing corrected output.json to workspace root.",
      "actions": [
        {
          "type": "write_file",
          "path": "output.json",
          "content": "{\n  \"status\": \"ok\",\n  \"version\": 2\n}\n"
        }
      ],
      "agent_claimed_done": true,
      "changed": true,
      "tokens": 0,
      "notes": "The fix — output.json now validates against schema.json. bl run reaches DONE on lap 1, keyless."
    },
    {
      "lap": "*",
      "agent_output": "Fix already applied; no further changes needed.",
      "actions": [{"type": "noop"}],
      "agent_claimed_done": true,
      "changed": false,
      "tokens": 0,
      "notes": "Fallback for any lap beyond 1 — should not be reached in the happy path."
    }
  ]
}
