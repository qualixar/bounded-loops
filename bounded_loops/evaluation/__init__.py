"""Mutant-corpus evaluation: measuring what an independent gate actually catches.

Shipped inside the package rather than under `scripts/` so that a reviewer who runs
`pip install bounded-loops` can regenerate the corpus and reproduce the paper's numbers. A
result that only the authors can rerun is not evidence.
"""
