# loops/bibliography-completeness/bounds.yaml
max_iterations: 10
no_progress_window: 3
max_tokens: null
max_wallclock_s: 60
sandbox: true
quarantine_inputs: true
schema: null
trace: true
# L1 keyless demo runs unattended so it can reach DONE offline; flip to true
# when wiring a real runner in front of a submission-ready paper.
require_approval: false
