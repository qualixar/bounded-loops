# loops/idoc-xml-schema/bounds.yaml
max_iterations: 10
no_progress_window: 3
max_tokens: null
max_wallclock_s: 60
sandbox: true
quarantine_inputs: true
schema: null
trace: true
# L2 in production: a passing structural check should gate human sign-off
# before an IDoc is posted into an SAP inbound queue, not auto-post. The
# keyless demo runs unattended so it can reach DONE offline; flip this to
# true when you wire a real runner.
require_approval: false
